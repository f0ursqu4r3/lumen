# Dependencies
node_modules/

# Build output
dist/
dist-ssr/
*.local

# Vite / Vitest caches
.vite/
coverage/

# Generated code (rebuilt via `bun run codegen`)
src/gitlab/generated/

# TypeScript incremental build info
*.tsbuildinfo

# Environment (secrets) — .env.example IS tracked
.env
.env.*
!.env.example

# Logs
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Electrobun build outputs (dist/ already ignored above)
/build

# Subagent persistent memory — not project source
.claude/agent-memory/

# Lumen Canary macOS arm64 build artifacts
/artifacts

# Cached SDL introspected from the live instance (reference; regenerate as needed)
src/gitlab/schema.graphql
